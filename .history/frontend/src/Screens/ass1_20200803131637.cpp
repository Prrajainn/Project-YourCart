#include<bits/stdc++.h>
using namespace std;
int main(){
    int arr[k];
    int k , m, count=0,result;
    cin>>k; cin>>m;
    while(k--){
        cin>>arr[count];
        count++; }
    sort(arr,arr+k);
    
return 0;
}